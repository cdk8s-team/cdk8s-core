# ~~ Generated by projen. To modify, edit .projenrc.js and run "npx projen".

name: debug-dotnet
on:
  push:
    branches:
      - epolon/debugging-dotnet-versions
jobs:
  debug:
    runs-on: ubuntu-latest
    permissions: none
    steps:
      - uses: actions/setup-dotnet@v2
        with:
          dotnet-version: 3.x
